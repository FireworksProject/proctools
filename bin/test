#!/bin/bash
THISDIR="$(cd `dirname "$0"` && pwd)"
ROOTDIR="$( dirname "$THISDIR" )"
DISTDIR="$ROOTDIR/dist"

fail () {
	echo "$@" >&2
	exit 1
}

coffee "$ROOTDIR/test/runtests.coffee" || fail "testing failed"
